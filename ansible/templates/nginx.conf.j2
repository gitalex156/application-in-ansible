server {
    listen 80;
    server_name {{ ansible_facts['hostname'] }};

    root /var/www/app;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
